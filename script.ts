
// Not exactly — while every valid JavaScript code is almost valid TypeScript, the reverse is not always true.

// Here’s the clarification:

// ✅ Yes — TypeScript is a superset of JavaScript
// That means:

// All valid JavaScript code is valid TypeScript code — unless TypeScript’s type-checking or compiler settings complain.

console.log("Hello, World!");

