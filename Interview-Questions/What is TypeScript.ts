
// What is TypeScript?

// Interview Answer:
// TypeScript is a strongly typed superset of JavaScript that adds static typing, interfaces, classes, and better tooling support.
// It compiles down to plain JavaScript so it can run anywhere JS runs.

let count: number = 10;